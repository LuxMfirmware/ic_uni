




(optional) ako su za zaluzine razliciti releji onda se koristi binary listener


kontroler samo sa roletnama (optinoal)
kontroler bez termostata
kontroler bez roletni


thermostat screen elements refresh is unsynchronised


kada se brzo pritisce dim svjetlo pocnu treperiti ostala dim svjetla, kada se sporo pritisce, nema tih problema


restart timers on brightness and color change

there can be miboxer with only dim, so you have to send status and not just brightness value

Light_Modbus_isNewValueOn() keeps getting called after statusSet()

za rgb svjetlo, ako se u funkciji DIMMER_SET_Listener() komentarise Light_Modbus_Brightness_Update_External() onda ce svjetlo raditi normalno zato sto esp vraca brightness kao 0, query se vise puta posalje sa kontrolera


Zadatak													Prioritet	Status					Bilješke
1. Uklanjanje "Magičnih Brojeva"						Visok		Značajno Napredovao		Završili smo refaktorisanje SVIH HandlePress... funkcija. Završili smo i Service_... i DSP_Init... funkcije za glavne ekrane. Preostaje da prođemo kroz preostale DSP_InitSet... funkcije za ekrane podešavanja (Settings 3, 4, 5, 6).
2. Poboljšanje Enkapsulacije							Visok		Završeno				Svi ključni moduli (lights, thermostat, ventilator, defroster, curtain) su sada potpuno enkapsulirani.
3. Refaktorisanje EEPROM upravljanja					Visok		Završeno				Svi moduli koriste _EepromConfig_t strukture sa CRC i magičnim brojem.
4. Optimizacija EEPROM Mape								Visok		Završeno				Mapa u stm32746g_eeprom.h je potpuno fleksibilna.
5. Unapređenje sistema prevođenja						Srednji		Nije započeto			Tabela sa prevodima (language_strings) je i dalje fiksno kodirana unutar display.c. Potrebno je izmjestiti je u zaseban fajl i dodati opciju za odabir jezika u meniju.
6. Centralizacija Widget ID-jeva						Srednji		Završeno				Uspješno je implementirana X-Macro tehnika.